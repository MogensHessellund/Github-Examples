name: environment-vars
on: [push]
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World"
        run:  echo "Hello  $github.actor og $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }} 
          MY_ACTOR: ${{ github.actor }} 
