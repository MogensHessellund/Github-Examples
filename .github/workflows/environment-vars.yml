name: environment-vars
on: [push]
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World"
        run:  echo "Hello  $MY_ACTOR and $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }} 
          MY_ACTOR: ${{ github.actor }} 
